
  # Gestión de productos y ventas

  This is a code bundle for Gestión de productos y ventas.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  